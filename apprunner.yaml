version: 1.0
runtime: python311

build:
  commands:
    build:
      - echo "App Runner build phase complete"

run:
  runtime-version: 3.11
  pre-run:
    - python3 -m pip install --upgrade pip
    - python3 -m pip install .
  command: ./startup.sh
  network:
    port: 8080
