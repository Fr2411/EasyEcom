import { PageShell } from '@/components/ui/page-shell';
import { ResetPlaceholder } from '@/components/ui/reset-placeholder';

export default function ProductsStockPage() {
  return (
    <PageShell title="Products & Stock" description="This legacy route now stays blank while the catalog foundation is rebuilt.">
      <ResetPlaceholder moduleName="Products & Stock" />
    </PageShell>
  );
}
