import { PageShell } from '@/components/ui/page-shell';

export default function ProductsStockPage() {
  return (
    <PageShell
      title="Products & Stock"
      description="Priority workspace shell for catalog and inventory flows. Feature slices will land here incrementally."
    >
      <div className="placeholder-card">
        <p>Initial production page shell is ready for products and stock modules.</p>
      </div>
    </PageShell>
  );
}
